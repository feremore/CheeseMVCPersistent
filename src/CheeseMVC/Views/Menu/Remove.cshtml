<h1>@ViewBag.title</h1>

<form method="post">

    @foreach (var menu in ViewBag.Menus)
    {
        <input type="checkbox" name="menuIds" id="@menu.ID" value="@menu.ID" />
        <label for="@menu.ID">@menu.Name</label>
        <br />
    }

    <input type="submit" value="Remove Selected Menu(s)" />
</form>